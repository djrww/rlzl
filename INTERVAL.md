# 區間運算濾波：事前規劃與成功率實測

`src/interval.rs` + `tests/interval.rs`（16 項，總計 **69 項全過**）。

**先講結論：成功率極高（99–100%），但在本專案的 i128 輸入下濾波是虧本的。**
只有當輸入本來就是 f64 時才真正獲利（1.95–2.14×）。下面是完整數據與原因。

---

## 一、三層架構

```text
Tier 0  事前規劃   由座標量級與謂詞次數，O(1) 決定路徑（不做任何算術）
Tier 1  執行期濾波  f64 近似 + 誤差界，若符號確定則直接採信
Tier 2  精確回退    i128（僅在 Tier 1 不確定時執行）
```

核心洞見：**謂詞只需要符號，不需要數值。** 若能證明 `det ≠ 0` 的符號，
就不必精確計算。這是 Shewchuk 自適應精度謂詞的思想。

---

## 二、Tier 0：事前規劃（純 O(1)，可在編譯期執行）

由 `bits ≈ degree · log₂(C) + log₂(terms) + 1` 估算，輸出四種決策：

| 謂詞 | C=1e3 | C=1e6 | C=1e9 | C=1e12 | C=1e18 |
|---|---|---|---|---|---|
| `orient2d` (次數2) | Float | Float | Filter | Filter | MayOverflow |
| `orient3d` (次數3) | Float | Filter | Filter | MayOverflow | **Bignum** |
| `incircle` (次數4) | Float | Filter | **Bignum** | Bignum | Bignum |
| `circ3` MEC (次數6) | Filter | **Bignum** | Bignum | Bignum | Bignum |

反推最大安全座標：

| 謂詞 | f64 (53bit) | i128 (127bit) |
|---|---|---|
| `orient2d` | 8.4×10⁶ | 1.2×10¹⁸ |
| `orient3d` | 3.3×10⁴ | 5.5×10¹¹ |
| `incircle` | 1.0×10³ | 2.7×10⁸ |
| `circ3` (MEC) | 1.3×10² | 5.2×10⁵ |

這些數字與 `GEOMETRY.md` 的實測邊界（incircle ~9×10⁸、MEC ~10⁶）同數量級，
互相印證。**規劃可在編譯期完成** —— 座標範圍已知時，`plan()` 是常數摺疊的。

---

## 三、成功率實測

### 濾波永不說謊（最重要的性質）

100 萬組隨機 `orient2d`：**濾波成功 999,996 次，錯誤 0 次。**
`incircle` 10 萬組同樣 0 錯誤。Cassini 災難性抵消案例中，
濾波**拒絕**而非附和浮點的錯誤答案。

這是可證明的性質，不是運氣：誤差界保證 `|det| > bound ⟹ 符號正確`。
最壞情況只是退化成「全部走精確路徑」，**絕不會給出錯誤答案**。

### 成功率 vs 資料退化程度（orient2d，各 20 萬組）

| 資料類型 | 成功率 | 回退次數 |
|---|---|---|
| 均勻隨機 ±1e3 | **100.00%** | 2 / 200,000 |
| 近共線（擾動 ±1） | **99.21%** | 1,547 |
| 網格 20×20 | **99.22%** | 3,524 |
| **完全共線** | **9.39%** | 181,220 |

### 成功率 vs 座標量級（incircle，均勻隨機）

C=1e3 到 C=1e10 **全部 100.00%**。

**結論：成功率只取決於資料是否退化，幾乎與座標量級無關。**
真實工程資料（CAD 圖框、網格）多半 ≥99%，只有刻意構造的完全退化才會崩到 9%。

---

## 四、⚠️ 效能實測：濾波在 i128 輸入下是虧本的

這是本次最重要的發現，也推翻了我上一輪「補濾波層能拿回 95% 速度」的建議。

### orient2d（每次呼叫，300 萬次平均）

| 資料 | i128 精確 | 區間濾波 | 靜態誤差界 | 最佳加速 |
|---|---|---|---|---|
| 均勻隨機 | **12.02 ns** | 126.87 ns | 53.10 ns | **0.23×** |
| 近共線 | **12.16 ns** | 126.40 ns | 53.36 ns | 0.23× |
| 完全共線 | **11.94 ns** | 146.83 ns | 50.63 ns | 0.24× |

**濾波比純精確慢 4 倍。** incircle 同樣（23.47 ns vs 58.97 ns，0.40×）。

### 為什麼？拆解後原因很清楚

| 操作 | 耗時 |
|---|---|
| **`i128 as f64` 轉換** | **6.70 ns** |
| `i64 as f64` 轉換 | 2.04 ns |
| i128 乘法 | 4.08 ns |
| f64 乘法 | 1.83 ns |
| **整個 i128 orient2d** | **5.83 ns** |

**單次 `i128 → f64` 轉換（6.70 ns）就比整個 i128 精確謂詞（5.83 ns）還貴。**
濾波需要 6 次轉換，光轉換就 40 ns，注定虧本。

i128 乘法只比 i64 慢 1.9×，甚至**比 f64 乘法還快**（現代 CPU 的 128 位整數乘法很便宜）。
這與「浮點快、大整數慢」的直覺完全相反。

### 濾波唯一能贏的場景：輸入本來就是 f64

CAD/GIS/圖形學的資料常態是 f64。此時快路徑無轉換成本：

| 資料 | 全精確（含轉換） | 濾波 | **加速** | 成功率 |
|---|---|---|---|---|
| 均勻隨機 | 39.86 ns | 20.44 ns | **1.95×** | 100.00% |
| 近共線 | 39.55 ns | 18.51 ns | **2.14×** | 99.21% |
| 完全共線（最壞） | 32.33 ns | 52.89 ns | **0.61×** | 9.39% |

**1.95–2.14× 加速，最壞退化到 0.61×。** 這才是濾波的真實價值 ——
遠低於文獻常說的「10× 以上」，因為那些比較的基準是 GMP/MPFR 這類
**任意精度**函式庫（慢好幾個數量級），而不是 i128。

> **i128 已經是一個極快的「精確算術」實作，快到讓濾波失去意義。**

---

## 五、意外發現：區間運算救回了 i128 的靜默溢位

`DOWNSTREAM.md` 記錄的靜默溢位案例（座標 1e9，交叉相乘需 176 bit）：

```
Rat::cmp(d1, d2)        → -1   ✗ 錯誤（靜默溢位）
ratcmp_will_overflow    → true ✓ 正確預測 i128 會溢位
ratcmp_interval(d1, d2) → Some(1) ✓ 正確！
```

真值為 `+1`（Python 任意精度驗證）。**區間濾波靠 f64 的指數範圍解決了 i128 解決不了的問題。**

原因：f64 指數範圍達 ±308 位十進位，遠超 i128 的 38 位。
兩者是**互補**而非替代 —— **i128 提供精度，f64 提供動態範圍**。

這讓 `ratcmp_adaptive` 的三層決策特別有價值：

```rust
1. 區間濾波確定  → 採信（可處理超出 i128 範圍的量級）
2. 事前規劃判定 i128 安全 → 走精確
3. 兩條路都不通 → 回傳 None（拒絕，而非靜默出錯）
```

---

## 六、建議：什麼時候該用濾波

| 情況 | 建議 |
|---|---|
| **輸入是 f64**（CAD/GIS/圖形） | ✅ 用 `orient2d_f64` —— 約 2× 加速 |
| **輸入是整數，座標在 i128 內** | ❌ **直接用 i128 精確版**，濾波只會拖慢 4× |
| **量級超出 i128**（如 176 bit 交叉相乘） | ✅ 用 `ratcmp_adaptive` —— 區間能救 |
| 資料高度退化（>50% 共線/共圓） | ⚠️ 濾波退化到 0.61×，考慮直接走精確 |
| 需要事前決策（選型/驗證） | ✅ 用 `plan()` —— O(1)，可編譯期執行 |

### 為什麼還是值得保留這個模組

1. **`plan()` 的事前規劃**是純收益 —— O(1) 成本，能在寫程式時就知道
   「這個座標範圍能不能用 i128」，避免 `DOWNSTREAM.md` 那種靜默溢位。
2. **區間運算的動態範圍**補足了 i128 的不足（見第五節）。
3. **f64 輸入路徑**在真實 CAD/GIS 場景有 2× 收益。
4. 濾波**永不說謊**已由 100 萬組測試驗證 —— 加它不會有正確性風險。

### 一句話

> 事前規劃（Tier 0）值得無條件加入；
> 執行期濾波（Tier 1）只在輸入為 f64 時才划算 ——
> **因為 i128 精確算術本身已經夠快，快到不需要被濾波保護。**
