# 實用性分析：甜蜜點、邊界線、最佳舞台

所有數字為本機實測（rustc 1.98.1 / 2 vCPU / 1.9 GB）。

## 0. 先講最重要的一個比值

| | 500,000 點 |
|---|---|
| 原生執行期 `welzl_slice` | **9.4 ms** |
| const-eval 編譯期 | **38,000 ms** |
| 比值 | **約 4,000×** |

const-eval 是一台 Miri 直譯器：無 SIMD、無暫存器配置、每個 i128 運算都走解釋迴圈。
**它永遠不會比執行期快。** 所以「編譯期算」的價值從來不是效能，而是三件事：

1. **執行期成本歸零** —— 二進位檔裡只剩一個常數，沒有程式碼、沒有記憶體配置
2. **錯誤左移** —— 幾何性質違反時**編譯失敗**，而不是上線後才 panic
3. **無執行期依賴** —— 不需要 allocator、不需要 libm、`no_std` 可用

如果你要的是「算得快」，這個工具是錯的答案。
如果你要的是「這個值必須在編譯期就正確且不佔執行期成本」，它就是對的。

---

## 1. 甜蜜點

| N | 淨編譯增量 | 判定 |
|---|---|---|
| ≤ 64 | **3–5 ms** | 完全免費，感知不到 |
| 65–256 | **14–26 ms** | ✅ **甜蜜點** —— 低於人類感知門檻 |
| 257–1,000 | 56 ms | ✅ 仍然舒適 |
| 1k–5k | 285 ms | ⚠️ 開始有感，需權衡 |
| 5k–20k | 569 ms | ⚠️ 每次 `cargo check` 都付這個代價 |
| 20k–500k | 0.6–38 s | ❌ 破壞開發迴圈 |
| > 500k | OOM | ❌ 不可用 |

**甜蜜點是 N ≤ 256。** 這個範圍不是隨便挑的 —— 它恰好覆蓋絕大多數
「編譯期就知道的幾何資料」的真實規模：

* 精靈碰撞外框的凸包頂點：10–50
* UI 元件錨點 / 圖示邊界：4–30
* 感測器陣列座標（機械手臂、LiDAR 標定板）：8–100
* 字型字符輪廓控制點：20–200
* PCB 元件焊盤群：10–150

**超過 1,000 個編譯期已知的點，本身就是罕見情境** —— 那個量級的資料通常
來自執行期輸入（相機、檔案、網路），而執行期輸入根本無法進 const-eval。

## 2. 邊界線

### 硬邊界（無法繞過）

**① 資料必須在編譯期已知 —— 這是唯一真正的天花板**

其他所有限制都可以調參數或改寫法，這一條不行。點來自使用者輸入、檔案、
網路、感測器？const-eval 一律無能為力。這條線劃掉了 MEC 的大多數經典應用
（電腦視覺、即時碰撞、聚類分析）。

**② 座標限於整數 ±10⁶（僅限 MEC；其他謂詞範圍大得多）**

`circ3` 的中間量約 C⁶，i128 上限 1.7·10³⁸ → C ≲ 3.4·10⁶。
浮點座標須先縮放取整（例如 mm → μm，保留 3 位小數仍有 ±1000 單位的空間）。
好消息：對「編譯期已知的固定幾何」而言，座標本來就多是整數格點或可精確縮放的定點數。

⚠️ 更正/補充：**±10⁶ 是 MEC（次數 6）專屬的限制，不是這套整數核心的普遍限制。**
`orient2d`（次數 2）可到 ~4×10¹⁸，`incircle`（次數 4，Delaunay/Voronoi 核心）可到 ~9×10⁸。
詳見 [`GEOMETRY.md`](GEOMETRY.md)。

**③ 無增量快取**

實測：N=5000 時，**只改一行無關程式碼，仍需 307 ms 全量重算**。
const-eval 結果不進 incremental cache。這使 N > 1000 的成本不是一次性的，
而是**乘以你每天 `cargo check` 的次數**。這條往往比絕對耗時更致命。

### 軟邊界（可調）

| 限制 | 預設 | 解法 |
|---|---|---|
| 純宏 tt-munching | 126 點 | `#![recursion_limit]` → 14,336（後為 SIGSEGV） |
| 遞迴 `welzl(P,R)` | 120 點 | 已由 `src/adaptive.rs` 解除 → 500,000 |
| `long_running_const_eval` | 59,392 點 | `#![allow(...)]` → 500 萬（後為 OOM） |

---

## 3. 最佳舞台：五個真實場景

按「是否真的比替代方案更好」排序，而非按酷炫程度。

### ★★★ 嵌入式 / `no_std` 固定幾何

最強的場合。`const fn` 不需 allocator、不需 libm、不需 `std`。
MCU 上要對固定感測器陣列做包覆圓判定，替代方案是：
(a) 手算後硬編碼魔術數字 —— 易錯、無法追溯；
(b) 執行期算 —— 吃 flash、吃 RAM、吃週期。

本方案兩者皆免，且座標改動時圓自動跟著更新。實測 `hit()` 反組譯：

```asm
hit:  movl $382, %r10d      ; cd 已烘焙
      addq $-6859, %rdi     ; cx 已烘焙
      ...                   ; 無 call、無迴圈、無配置
```

### ★★★ 資產管線的編譯期烘焙

遊戲精靈的 broad-phase 碰撞圓、GUI 元件的命中測試邊界。
頂點數 10–50 → 落在甜蜜點正中央。傳統做法是離線工具生成 `.rs`/`.bin`
再 include，本方案省掉整個建置步驟與同步問題：改頂點，圓自動重算。

### ★★★ 編譯期不變量驗證（可能是最被低估的用途）

這其實不是「算圓」，是「用圓來斷言」：

```rust
const _: () = assert!(cmp_radius(HULL, MAX_ALLOWED) <= 0);  // 超框則編譯失敗
```

零件不得超出封裝範圍、UI 元件不得溢出安全區、天線陣列須落在規範圓內 ——
把這些從「執行期檢查 / 人工 code review」變成**編譯器強制**。
`examples/demo.rs` 裡三種實作交叉比對的 `const _: () = assert!(...)` 就是這個模式。

### ★★ 測試預言（test oracle）

O(n³) 暴力法在 const-eval 中的緩慢反而無所謂 —— 測試本來就在 CI 跑。
本專案自己就用了這招：300 組隨機點集 × 暴力法比對。

### ★ 純編譯期大規模求解

500k 點、38 秒。技術上成立，實務上幾乎找不到正當理由 ——
那個規模的資料通常來自執行期。**這是能力展示，不是應用建議。**

---

## 4. 但真正可複用的資產，其實不是「編譯期」

誠實的結論：這個專案裡最有價值的部分，是**副產品**。

### `src/exact.rs` —— i128 精確有理數幾何核心

它是 `const fn`，所以**編譯期與執行期都能用同一份程式碼**（本文的 runtime
benchmark 就是直接呼叫同一個 `welzl_slice`，9.4 ms / 500k）。

而它解決的是一個與編譯期完全無關的真實痛點：**浮點幾何的健壯性**。
標準 MEC 實作在近共線、近共圓、座標量級懸殊時會因浮點誤差給出錯誤拓撲判定
（點在圓內還是圓外？）。本實作的判定是純整數比較：

```text
(p.x·cd − cx)² + (p.y·cd − cy)² ≤ r2
```

**不存在 epsilon，不存在誤差累積，不存在退化判定失敗。**
CAD/CAM、PCB 設計規則檢查、GIS、計算幾何研究 —— 這些領域裡
「答案必須可證明正確」比「快 20%」重要得多。

### 值得借鑑的模式：燃料 + 續傳（`src/adaptive.rs`）

「遞迴攜帶預算，耗盡前主動移交給等價的迭代解法」——
這個模式適用於任何 **深度受限但問題等價可轉換** 的場景，
不限於 const-eval：WASM 淺堆疊、深度遞迴的 parser、需要可中斷的長任務。
關鍵前提是找到那個「同一個數學物件的另一種解法」（本例是 `MEC(P | R ⊂ ∂D)`）。

---

## 5. 一句話決策表

| 你的情況 | 建議 |
|---|---|
| 點在編譯期已知，N ≤ 256，要零執行期成本 | ✅ **正是為你設計的** |
| 想用幾何性質做編譯期斷言 | ✅ **最被低估的用途** |
| `no_std` / MCU 固定幾何 | ✅ 難有更好的替代 |
| N 在 1k–20k 且會頻繁編輯 | ⚠️ 算一下：淨增量 × 每日編譯次數 |
| 點來自執行期輸入 | ❌ 用 `welzl_slice`，別碰 const |
| 追求最快 | ❌ 執行期快 4,000 倍 |
| 需要浮點座標 / 高精度輸入 | ⚠️ 先縮放成 ±10⁶ 內的定點整數 |
| **只想要健壯無誤差的幾何判定** | ✅ **直接用 `exact.rs`，忽略編譯期那層** |
