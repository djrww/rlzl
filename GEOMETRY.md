# 精確幾何核心：能延伸到半平面交 / Voronoi / 三角剖分嗎？

**能，而且 Delaunay/Voronoi 的適用範圍比 MEC 更大。**
但半平面交有一個本質差異，必須分開談。以下全部為實測（`tests/geometry.rs`，13 項全過）。

---

## 1. 先回答「是否成熟」

**核心是成熟的；套件不是。**

已達生產品質：
- `exact.rs` 的有理數表示與約分 —— 完備、無誤差、`const fn` 雙用
- `predicates.rs` 的三個謂詞 —— 這是計算幾何的**標準地基**（Shewchuk 的經典工作即此）
- 32 項測試，含 300 組 MEC vs 暴力法、80 組 Delaunay 嚴格驗證、Cassini 災難性抵消

尚未達生產品質：
- **算法複雜度**：`delaunay_brute` 是 O(n⁴)，只適合 n ≲ 100 或當測試預言
- **API 表面**：無 crate 發布、無 `no_std` 標註、無 2D/3D 泛化
- **缺自適應精度**：Shewchuk 的做法是「先用浮點濾波，僅在接近 0 時才走精確路徑」，
  能有 95% 以上的情況跑浮點速度。本實作**恆走精確路徑**，慢但簡單

準確說法：**一個正確且經驗證的精確謂詞核心，可作為上層算法的地基，但還不是一個成熟的幾何庫。**

---

## 2. 關鍵發現：次數（degree）決定一切，而 MEC 是最貴的

精確謂詞的座標上界由它在座標中的**多項式次數** d 決定：中間量 ≈ C^d < 2¹²⁷。

| 謂詞 | 用途 | 次數 | i128 座標上界 | 相對 MEC |
|------|------|------|---------------|----------|
| `hp_contains` | 半平面內外 | 1 | ~10³⁸ | — |
| `orient2d` | 凸包、剖分方向 | 2 | **~4×10¹⁸** | ×4×10¹² |
| `hp_side_of_intersection` | 半平面交 | 3 | ~10¹² | ×10⁶ |
| **`incircle`** | **Delaunay / Voronoi** | **4** | **~9×10⁸** | **×900** |
| `circ3`（MEC） | 最小包覆圓 | 6 | ~10⁶ | ×1 |

**反直覺的結論：Delaunay/Voronoi 能用的座標範圍比 MEC 大約 900 倍。**
`APPLICABILITY.md` 裡「座標限 ±10⁶」是 MEC 專屬的限制，
不是這套整數核心的普遍限制。1e8 座標的 Delaunay 已實測通過。

---

## 3. 為什麼 Delaunay/Voronoi 特別適合 —— 「次數不累積」

這是整件事的理論核心。

Voronoi 頂點是**有理數**（外接圓心 `(ux/d, uy/d)`）。直覺上，
拿有理數頂點再做後續判定，分母會層層累積直到溢位。**但實際上不會**，因為：

> Delaunay / Voronoi / 凸包的**所有**拓撲判定，都能化約成**原始輸入座標**的行列式。

判定「這條邊該不該翻轉」用的是 `incircle(a,b,c,d)` —— 四個**輸入點**，
而不是「先算出外接圓心，再判斷距離」。衍生的有理數頂點只在**最終輸出**時出現，
從不回饋成下一步判定的輸入。

實測佐證：座標 1e8、5 點的 Voronoi，最大分母 27 bit，**不隨點數增長**。

這就是為什麼 Delaunay 能用定長整數（i128 甚至 i64）精確計算，
也是 CGAL / Triangle / Shewchuk 這類函式庫的共同基礎。

### 精確謂詞在此的真正價值：不是「更準」，是「不崩潰」

浮點 `incircle` 誤判的後果不是「答案差一點」，而是：
- **無限翻轉迴圈**（A 說該翻、B 說不該翻，來回震盪）
- **非平面圖輸出**（三角形重疊、洞、拓撲不一致）
- 這些是計算幾何實作最常見的 crash 來源

實測：5×5 完美網格（大量四點共圓）與 9 點全共圓，
精確版嚴格通過 Delaunay 檢查（**無任何點落在任何外接圓內**）。

### 實測：浮點確實會錯，但需要真正的退化才觸發

誠實記錄一個負面結果：**60 萬組隨機大座標測試中，浮點誤判 0 次。**
隨機取樣幾乎不可能撞上真退化。必須刻意構造才能觸發：

**Cassini 恆等式** `F(n+1)·F(n-1) − F(n)² = ±1` —— 真值恆為 ±1，
但座標 ~10⁹ 時乘積 ~10¹⁸ 遠超 2⁵³：

```
F=102334155    exact=-1  float=-1   一致
F=165580141    exact=+1  float=2    一致（值錯但符號對）
F=267914296    exact=-1  float=0    *** 誤判：真值 ±1 卻報「共線」***
F=433494437    exact=+1  float=0    *** 誤判 ***
F=701408733    exact=-1  float=0    *** 誤判 ***
...
16 組中浮點誤判 6 組
```

浮點報 `0` 意味「三點共線」—— 這正是會讓三角剖分產生退化三角形、
進而拓撲崩壞的錯誤。**這才是精確謂詞真正在防的東西。**

實務意義：浮點在「一般資料」上多半沒事，
但在**網格、CAD 圖框、規則排列、對稱結構**這類真實工程資料上，退化是常態而非例外。

---

## 4. 半平面交：唯一會「次數累積」的算法 —— 以及解法

半平面交與前兩者有**本質差異**：Sutherland–Hodgman 裁剪會把
「上一次的交點」當成「下一次的輸入」，於是次數真的會累積。

實測（未約分）：

| 裁剪次數 | 最大分母 |
|---|---|
| 1 | 17 bit |
| 2 | 49 bit |
| 3 | 81 bit |
| 4 | **127 bit（i128 飽和）** |

**每次裁剪約 +32 bit，i128 只夠 4 次。** 這是真正的硬邊界。

### 解法一：每步約分（已實作，效果驚人）

`RatPt::reduce()` 在每次裁剪後對 `(x, y, w)` 取 GCD 約分：

| 裁剪次數 | 未約分 | **約分後** |
|---|---|---|
| 1 | 17 bit | **1 bit** |
| 2 | 49 bit | **4 bit** |
| 3 | 81 bit | **5 bit** |
| 4 | 127 bit | **6 bit** |
| 8 | 溢位風險 | **5 bit** |

**127 bit → 6 bit。** 因為裁剪產生的分子分母帶有大量公因數。
測試 `halfplane_denominator_growth_controlled` 斷言 8 次裁剪後仍 < 40 bit。

⚠️ 但這是**啟發式而非保證**：對抗性輸入（互質的大係數）仍會膨脹。

### 解法二：改用不累積的算法（推薦）

半平面交的標準做法是**按角度排序 + 單調佇列**（O(n log n)）。
它的每個判定都只涉及**三條原始直線**（`hp_side_of_intersection`，次數 3），
交點只在最終輸出時計算一次 —— **回到「次數不累積」的性質**。

本專案已提供這個謂詞。這也印證了通則：
> 精確整數幾何的關鍵，不在於用多大的整數型別，
> 而在於**把算法組織成「所有判定都只依賴原始輸入」的形式**。

---

## 5. 總結表

| 算法 | 可行性 | 座標上界 | 次數累積？ | 備註 |
|---|---|---|---|---|
| 凸包 / 方向判定 | ✅ 極佳 | ~4×10¹⁸ | 否 | 次數 2，範圍極寬 |
| **Delaunay 三角剖分** | ✅ **極佳** | **~9×10⁸** | **否** | 精確 incircle 消除翻轉迴圈 |
| **Voronoi 圖** | ✅ **極佳** | **~9×10⁸** | **否** | Delaunay 對偶，頂點為精確有理數 |
| 半平面交（裁剪式） | ⚠️ 需約分 | 依裁剪次數 | **是** | 約分後 8 次仍 < 40 bit |
| 半平面交（排序式） | ✅ 佳 | ~10¹² | 否 | 用 `hp_side_of_intersection` |
| 最小包覆圓 | ✅ 良 | ~10⁶ | 否 | 次數 6，最貴 |
| 反覆 offset / CSG | ❌ | — | **是（無界）** | 需 bignum，定長整數無解 |

## 6. 最誠實的建議

**如果你要做的是 Delaunay/Voronoi：** 這套核心是對的地基，
且座標範圍（1e8）足以覆蓋絕大多數工程應用（μm 精度下 100 公尺見方）。
但別用本專案的 O(n⁴) 實作 —— 把它當測試預言，上層換成 Bowyer–Watson 或 CGAL。

**如果你要的是生產級效能：** 加一層**浮點濾波**（先用 f64 算，
若 |det| > 誤差上界就直接採信，否則才走 i128）。這能拿回 95% 的浮點速度
而不損失正確性 —— 這是 Shewchuk 自適應精度謂詞的核心思想，也是本專案最值得的下一步。

**如果你的資料本來就是浮點：** 先確定能否縮放成整數（多數 CAD/PCB/GIS 可以，
它們本質是定點數）。若真的需要任意精度浮點，就得換 bignum，
那時 i128 的優勢（無配置、`const fn`、快）就不存在了。
