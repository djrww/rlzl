# 下游應用實測：線段最近距離 / 球面凸包面積 / 布爾運算

把 `exact.rs` + `predicates.rs` 當作底層，實作三個上層算法並實測邊界。
`src/segdist.rs`、`src/sphere.rs`、`src/boolops.rs`，21 項新測試（總計 **53 項全過**）。

三個應用剛好落在**三個不同的難度層級**，這比「都能做」有用得多。

---

## ⚠️ 最重要的發現：靜默溢位（必讀）

實測中發現一個**會導致錯誤答案卻不會 panic** 的安全問題：

```
C=1e6:  cmp=1   交叉相乘需 116 bit   OK
C=1e8:  cmp=1   需 156 bit  → i128 溢位
C=1e12: cmp=1   需 231 bit  → 溢位，實測回傳負數
```

真值恆為 `1`（Python 任意精度驗證），但 i128 版在 C ≥ 1e8 時**靜默給出錯誤答案**。

原因有兩層，第二層特別隱蔽：

1. Rust release 模式的整數溢位是 **wrapping**，不 panic
2. **`-C debug-assertions` 不跨 crate 邊界生效** —— 即使測試程式開了溢位檢查，
   函式庫本身是以 release 編譯的，溢位仍靜默發生

這意味著前幾份文件裡「用 `debug-assertions` 測出上界」的方法**對跨 crate 呼叫是失效的**。
本次改用 **Python 任意精度計算真值** 反查所需位元數，才得到可信的邊界。

**對策**：已加入 `Rat::cmp_checked()`，溢位時回傳 `None` 而非錯誤答案。
生產程式碼應一律使用 checked 版本 —— **寧可回報「算不了」，也不要靜默回傳錯誤的拓撲判定**。

---

## 應用一：線段最近距離 ✅ 理想契合

### 為什麼契合：距離是無理數，但**判定只需要距離平方**

```text
dist²(p, ab) = cross(p−a, b−a)² / |b−a|²    （垂足在段內）
             = min(|p−a|², |p−b|²)           （垂足在段外）
```

距離平方是**有理數**，完全落在 i128 能力內。這是「避免開方」經典技巧的精確版：
不只避免浮點誤差，而是讓結果成為**可精確比較**的有理數。

實測：點 (0,0) 到線段 (1,0)-(0,1) 得 `1/2` —— 精確的二分之一，不是 0.4999999999999999。

### 分層邊界（實測）

| 操作 | 次數 | 座標上界 | 說明 |
|---|---|---|---|
| 相交判定（`orient2d`×4） | 2 | **≥1e18** | 實測 1e18 仍正確 |
| 與**常數門檻**比較 | 4 | ~1e9 | `within_distance2` |
| **比較兩個距離²** | 6 | **~1e6** | 交叉相乘，最貴 |

**實務建議**：多數場景（碰撞偵測、鄰近查詢）只需要「是否 < 門檻」，
用 `within_distance2` 就能到 1e9。只有排序/找最近對才需要互相比較，那時才受限於 1e6。

退化情形全數精確：端點接觸、T 型接觸、共線重疊 —— 全部回傳精確 0，**不需要 ε**。
2000 組隨機比對浮點參考實作，全部一致。

---

## 應用二：球面凸包面積 ⚠️ 撞到本質障礙（但可分層搶救）

### 障礙：球面面積是**超越數**，不是有理數

球面三角形面積 = 球面盈餘 `E = A + B + C − π`。π 與各角皆為超越數。
**不存在任何整數/有理數表示能承載它** —— 這不是 i128 位寬不夠，換 bignum 也一樣。

這與其他所有應用形成鮮明對比：MEC 半徑²、多邊形面積、距離² 都是有理數，
球面面積**根本不在有理數域內**。

### 但可精確的部分遠比想像多 —— 三層拆解

| 層次 | 內容 | 精確？ |
|---|---|---|
| **組合層** | 哪些點在凸包上、面的鄰接、是否含球心 | ✅ 完全精確（`orient3d`，次數 3，上界 ~1e12） |
| **代數層** | `tan(Ω/2)` 立體角半角正切 | ✅ **完全精確的有理數** |
| **超越層** | `Ω = 2·arctan(...)`、總面積 | ❌ 必須浮點 |

代數層的關鍵是 **Van Oosterom–Strackee 公式**。一般情形分母含 `|a|,|b|,|c|`（無理），
但若所有點落在**整數半徑的球面**上（`|a|=|b|=|c|=R`）：

```text
tan(Ω/2) = |det(a,b,c)| / ( R·(R² + a·b + a·c + b·c) )
```

分子分母皆為整數 → **精確有理數**。

實測（r=25 整數球面）：

```
(-25,0,0),(-24,-7,0),(-24,0,7)  ->  tan(Ω/2) = 1/49
(-25,0,0),(-24,0,7),(-24,0,-7)  ->  tan(Ω/2) = 0/1      ← 精確偵測共面（Ω=0）
(-25,0,0),(-24,0,-7),(-24,7,0)  ->  tan(Ω/2) = -1/49
```

實務意義：立體角的**比較、是否為零、加總的代數結構**都可精確進行，
只有最終要「以球面度為單位的數值」時才需要 `arctan`。
整數點球面在晶格模型、測地網格、方向取樣中很常見。

### 過程中抓到的真實 bug

初版八個八分體加總得 **8π 而非 4π**。原因是用了 `atan(num/den)` ——
單純相除會**丟失分母的符號**，導致 `Ω > π`（鈍角球面三角形，`den < 0`）
的分支被錯誤折回銳角。改用 `atan2(|num|, den)` 後正確。

值得記錄的是：**代數層一直是精確的，錯的是通往超越層的那一步轉換**。
這正好印證分層的價值 —— 精確核心沒問題時，錯誤只可能出在浮點介面處，範圍很小、容易定位。

---

## 應用三：多邊形布爾運算 ✅ 判定完美 / ⚠️ 構造需留意

### 分離「判定」與「構造」

* **判定層** —— 只依賴原始整數座標，次數 2，**永不累積、完全精確**
* **構造層** —— 交點是有理數，鏈式運算時分母會累積

這個分離讓「A 與 B 是否重疊」「點是否在結果內」這類**謂詞式查詢**
可以無條件精確回答，即使完整輸出多邊形難以維持整數表示。

### Shoelace 面積：整數幾何最漂亮的性質

多邊形面積的**兩倍恆為整數**（Pick 定理的基礎），因此浮點誤差**根本不存在**。
實測到 C=1e18 仍精確：

```
C=1e18: area2 = 2000000000000000000000000000000000000  (精確整數)
```

### 點在多邊形內：沒有「射線穿過頂點」的經典 bug

用 `orient2d` 決定交叉方向，而非比較浮點交點座標。實測對角菱形，
射線恰好穿過左右頂點的情形（浮點實作最常出錯處）判定正確。
且能區分**內部 / 邊界上 / 外部**三態 —— 邊界是精確的，不需 ε。

### 鏈式 CSG 的分母累積（實測）

真正保持有理頂點、不回退整數的鏈式裁剪：

| | 未約分 | 約分後 |
|---|---|---|
| 8 次裁剪後最大分母 | 21 bit | **9 bit** |

比 `GEOMETRY.md` 裡半平面交的情況（127 bit 飽和）好得多，原因是
矩形/凸多邊形裁剪產生的交點帶有大量公因數。
但這**仍是啟發式**：對抗性輸入（互質大係數、深度鏈式）仍會膨脹。

生產級 CSG 的標準解法是 **snap-rounding**（把交點吸附回整數格點），
用可控的幾何誤差換取表示的封閉性。

---

## 總結：三個應用，三種結論

| 應用 | 契合度 | 精確範圍 | 關鍵限制 |
|---|---|---|---|
| **線段最近距離** | ✅ **理想** | 相交 1e18 / 門檻 1e9 / 比較 1e6 | 交叉相乘次數 6 |
| **球面凸包面積** | ⚠️ **分層** | 組合層 1e12、`tan(Ω/2)` 精確 | **面積是超越數，數學上不可能精確** |
| **多邊形布爾** | ✅ **判定完美** | 面積/判定 1e18 | 鏈式構造需約分或 snap-rounding |

### 貫穿三者的通則

1. **問「答案在哪個數域」比問「位寬夠不夠」更重要。**
   有理數（距離²、面積、`tan(Ω/2)`）→ 可精確；超越數（球面面積、角度）→ 不可能。

2. **判定與構造要分開。** 判定通常能化約成原始輸入的行列式（次數固定、不累積），
   構造則會產生衍生量。把 API 設計成「盡量只暴露判定」，精確性就能免費獲得。

3. **靜默溢位是這類函式庫最危險的失效模式。**
   一律提供 `checked` 版本，並用任意精度工具（Python/GMP）反查真實位元需求，
   不要依賴 `debug-assertions`（它不跨 crate）。
